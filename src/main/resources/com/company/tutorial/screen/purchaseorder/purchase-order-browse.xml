<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<window xmlns="http://jmix.io/schema/ui/window"
        xmlns:c="http://jmix.io/schema/ui/jpql-condition"
        caption="msg://purchaseOrderBrowse.caption"
        focusComponent="purchaseOrdersTable">
    <data readOnly="true">
        <collection id="purchaseOrdersDc"
                    class="com.company.tutorial.entity.PurchaseOrder">
            <fetchPlan extends="_base"/>
            <loader id="purchaseOrdersDl">
                <query>
                    <![CDATA[select e from PurchaseOrder e]]>
                </query>
            </loader>
        </collection>
    </data>
    <facets>
        <dataLoadCoordinator auto="true"/>
        <screenSettings id="settingsFacet" auto="true"/>
    </facets>
    <actions>
        <action id="lookupSelectAction"
                caption="msg:///actions.Select"
                icon="LOOKUP_OK"
                primary="true"
                shortcut="${COMMIT_SHORTCUT}"/>
        <action id="lookupCancelAction"
                caption="msg:///actions.Cancel"
                icon="LOOKUP_CANCEL"/>
    </actions>
    <dialogMode height="600"
                width="800"/>
    <layout expand="purchaseOrdersTable" spacing="true">
        <filter id="filter"
                dataLoader="purchaseOrdersDl">
            <properties include=".*"/>
        </filter>
        <groupTable id="purchaseOrdersTable"
                    width="100%"
                    dataContainer="purchaseOrdersDc">
            <actions>
                <action id="create" type="create"/>
                <action id="edit" type="edit"/>
                <action id="remove" type="remove"/>
            </actions>
            <columns>
                <column id="status"/>
                <column id="orderNumber"/>
            </columns>
            <simplePagination/>
            <buttonsPanel id="buttonsPanel"
                          alwaysVisible="true">
                <button id="createBtn" action="purchaseOrdersTable.create"/>
                <button id="editBtn" action="purchaseOrdersTable.edit"/>
                <button id="removeBtn" action="purchaseOrdersTable.remove"/>
            </buttonsPanel>
        </groupTable>
        <hbox id="lookupActions" spacing="true" visible="false">
            <button action="lookupSelectAction"/>
            <button action="lookupCancelAction"/>
        </hbox>
    </layout>
</window>
